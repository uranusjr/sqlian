[tox]
envlist = py27, py34, py35, py36

[testenv]
setenv =
    PYTHONPATH = {toxinidir}
deps =
    pipenv
commands =
    pipenv install
    pipenv run pip install pytest pytest-cov pytest-pythonpath
    pipenv run pytest {toxinidir}/tests
